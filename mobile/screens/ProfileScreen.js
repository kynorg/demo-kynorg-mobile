import React from "react";
import { View, Text } from 'react-native';

const ProfileScreen = ({ navigation }) => {
    return (
        <View>
            <Text>Change Account Info</Text>
        </View>
    );
};
export default ProfileScreen;
